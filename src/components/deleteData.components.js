import React,{Component} from "react";
import { Link } from "react-router-dom";

export default class deleteData extends Component{
    render(){
        return(
            <div className="container">Deletion</div>
        )
    }
}